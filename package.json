{
  "name": "cheerslk",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "clean": "turbo clean",
    "dev:customer": "turbo dev --filter=@cheerslk/customer",
    "dev:rider": "turbo dev --filter=@cheerslk/rider",
    "dev:admin": "turbo dev --filter=@cheerslk/admin"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.7.0"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
